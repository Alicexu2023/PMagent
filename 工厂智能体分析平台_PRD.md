# 工厂智能体分析平台 PRD

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 产品名称 | 工厂智能体分析平台 |
| 文档版本 | V2.1 |
| 文档日期 | 2026-08-14 |
| 产品定位 | 内部 AI 产品分析与日常工作提效工具 |
| 主要用户 | 产品经理、产品运营 |
| 核心人群 | 缺少数据分析经验的新手产品经理 |
| 交付方式 | 完整范围一次开发、一次上线、统一验收 |
| 数据方式 | CSV、Excel、JSON 离线导入，不接神策 API |
| AI 模型 | 支持 GPT、DeepSeek、Qwen |
| 部署方式 | 安装在目标 Windows 电脑，本机通过 `127.0.0.1` 访问，不需要域名 |
| 访问范围 | 仅目标电脑本机使用，不提供局域网或公网访问 |

## 2. 产品定位

工厂智能体分析平台是产品和运营团队内部使用的 AI 分析工具。它帮助产品经理，特别是缺少数据经验的新手，完成从数据导入、选择分析方法、解读指标，到形成产品结论和下一步动作的全过程。

平台不面向外部客户售卖，不追求通用 BI 工具的全部能力。它聚焦工厂智能体的日常产品分析，以“小白也能用、结论有证据、建议能执行”为核心原则。

### 2.1 部署与访问边界

- 平台安装在指定的 Windows 目标电脑，使用该电脑的浏览器打开 `http://127.0.0.1:8000`。
- `127.0.0.1` 仅代表当前电脑。其他电脑无法通过这个地址访问目标电脑上的平台。
- 本次不购买域名，不配置公网入口，不开放局域网端口，不安装 Docker。
- 平台数据保存在目标电脑本地，建议使用 `D:\FactoryAgentData`，并支持手工备份和迁移。
- CSV、XLSX、JSON 文件在本地导入和计算；调用 GPT、DeepSeek、Qwen 云端 API 时，只有经过脱敏且完成分析所需的最少文本会发送到模型服务商。
- 若未来改为多人从各自电脑访问，需要单独增加内网地址、账号权限、HTTPS 和服务器运维方案，不属于本次交付范围。

## 3. 项目背景

产品和运营团队在日常工作中需要持续回答以下问题：

- 用户最常问什么，有哪些真实问法和口语表达。
- 智能体对哪些问题识别不准，用户为什么会重复提问。
- 用户从进入智能体到获得答案、采取后续动作，主要流失在哪一步。
- 哪些用户在持续使用，哪些用户试用后没有再回来。
- 新功能上线后是否被使用，是否改善了用户体验。
- 下一个版本应该优先解决什么问题。

当前这类分析依赖个人经验和手工整理。新手产品经理常见困难包括：不知道该选什么方法、不会定义指标、不会判断差异是否值得关注，以及分析后无法形成可执行动作。

## 4. 产品目标

### 4.1 核心目标

1. 让不会 SQL、不熟悉产品分析方法的产品经理也能独立完成常用分析。
2. 用 AI 辅助选择分析方法、解释结果、生成结论和下一步行动。
3. 建立工厂智能体的高频问题、真实问法和同义表达字典。
4. 建立漏斗、路径、留存、用户分群、功能采用、问题效果和异常诊断的统一分析方法。
5. 将“看数据”变成“发现问题 - 形成判断 - 执行动作 - 回看效果”的工作闭环。

### 4.2 效率目标

- 标准数据导入后，用户可在 30 分钟内获得首份可阅读分析结果。
- 产品经理无需编写 SQL 或代码即可完成核心分析。
- 周报、专项分析和上线后复盘可由系统生成初稿，用户只需复核和补充业务背景。
- 每条重要结论均能追溯到指标、对比和代表样本。

## 5. 非目标

- 不作为对外售卖的 SaaS 产品，不设计商业化、客户购买、租户计费等能力。
- 不做通用 BI 平台，不支持任意数据源和任意图表自由搭建。
- 不接入神策 API，不自动从神策拉取数据。
- 不提供域名、公网访问、局域网共享访问和多电脑同时使用。
- 不建设多租户、企业 SSO 或复杂账号权限体系；目标电脑及其 Windows 账号承担第一层访问控制。
- 不用 AI 替代产品经理做最终决策，AI 负责提供数据证据、解释和建议。
- 不在无人审核的情况下自动修改线上意图、提示词或知识库。
- 不仅凭 AI 生成转化率、留存率等客观指标；指标必须由导入数据计算得出。

## 6. 目标用户

| 用户 | 主要困难 | 平台帮助 |
|---|---|---|
| 新手产品经理 | 不知道选什么分析方法，不会解读数据 | 通过任务引导和 AI 解释完成分析 |
| 产品经理 | 手工整理耗时，结论和证据分散 | 自动统计、下钻样本、生成报告和行动 |
| 产品运营 | 难以快速发现高频需求、新问题和异常变化 | 提供问题洞察、用户分群、趋势和异常诊断 |

本产品的业务用户仅包含产品和运营。目标电脑的指定使用者同时承担本机管理员职责，维护模型密钥、数据目录和指标口径；本次不单独建设登录和角色管理。

## 7. 核心使用场景

### 7.1 智能体问答质量分析

产品经理导入问答明细，查看高频问题、前 10～20 个真实问法、识别失败样本和重复提问，形成同义表达和意图优化清单。

### 7.2 新功能上线复盘

产品经理选择功能上线时间和目标用户，AI 建议应查看的功能采用、漏斗、留存和用户分群，并生成“是否达到目标”的复盘初稿。

### 7.3 用户流失定位

用户输入“为什么用户提问后没有继续操作”，系统引导配置漏斗和路径，定位流失环节、主要问题和影响用户。

### 7.4 用户群体差异分析

产品或运营对比新老用户、不同岗位、区域、客户类型等群体的使用频次、问题偏好、回答效果和留存差异。

### 7.5 周报和专项报告

用户选择时间和分析范围，系统自动汇总关键趋势、高频问题、主要异常、结论和建议动作，生成可编辑的报告初稿。

## 8. 产品设计原则

1. **任务导向**：首页直接进入分析工作，不做宣传式首页。
2. **新手优先**：用业务问题引导用户，不要求用户先理解专业方法。
3. **AI 辅助而非黑盒**：AI 必须说明使用了什么数据、得出什么判断、哪些只是假设。
4. **先结论后详情**：页面和报告先展示发现、影响和动作，再展示指标和样本。
5. **客观数值由系统计算**：AI 仅解释和归纳，不编造数值。
6. **结果可修改**：用户可修改聚类、标准问题、结论和建议，保留人的最终判断。

## 9. 整体工作流程

1. 用户创建分析任务，选择“问答质量”“功能上线复盘”“用户流失”“用户分群”或“自定义问题”。
2. 导入 CSV、Excel 或 JSON 数据。
3. 系统检查字段、数据量、时间范围、重复值、空值和可分析范围。
4. AI 根据用户的业务问题推荐分析方法、指标和对比维度。
5. 用户确认口径后，系统计算客观指标，AI 进行语义聚类和结果解读。
6. 系统输出结论、数据证据、影响、下一步动作和验证方式。
7. 用户复核、修改并导出报告或行动清单。

## 10. 功能需求

### 10.1 AI 分析工作台

工作台是产品首页，直接展示：

- 新建分析任务。
- 最近分析和待继续任务。
- 本周主要异常和待处理动作。
- 问答质量、漏斗、留存、功能采用和用户分群等常用模板。

用户可直接输入业务问题，例如“帮我看看这周智能体为什么重复提问变多了”。AI 将问题转换为分析任务，但在运行前必须让用户确认时间、人群、指标和数据范围。

### 10.2 分析任务引导

针对新手产品经理，系统通过三个问题完成分析配置：

1. 你想回答什么业务问题？
2. 你关注哪段时间和哪类用户？
3. 你希望与什么进行对比？

AI 根据回答推荐方法，并用业务语言说明“为什么选这个方法”和“还缺什么数据”。用户可直接接受建议，也可手动调整。

### 10.3 数据导入与质量检查

- 支持 CSV、XLSX 和 JSON。
- 支持拖拽上传和导入历史查看。
- 支持字段自动识别和手动映射。
- 检查必填字段、时间格式、重复记录、空值、异常值和时间范围。
- 导入完成后明确告知“能做哪些分析”和“哪些分析因数据不足无法完成”。
- 生成数据质量报告，显示原始记录数、有效记录数、排除数和原因。
- 支持保存导入模板，用于后续周报和重复分析。

### 10.4 高频问题与真实问法

建立“关键词 - 标准问题 - 真实问法”三层分析结构：

| 层级 | 作用 | 示例 |
|---|---|---|
| 关键词 | 了解用户关注主题 | 报警、设备、工单 |
| 标准问题 | 归并同一业务意图 | “如何查看设备报警？” |
| 真实问法 | 保留用户实际表达 | “哪里看机器报警” |

每个高频标准问题展示：

- 提问次数、提问人数、会话数、占比和趋势。
- 前 10～20 个真实问法，默认显示 20 个。
- 每个真实问法的次数、人数、当前识别意图、置信度和回答状态。
- 识别成功率、有效回答率、重复提问率和后续转化。

真实问法榜单需同时考虑频次和表达差异，避免被大量几乎相同的句子占据。用户可人工合并、拆分、重命名和调整归属。

### 10.5 同义表达和意图诊断

AI 基于高频真实问法生成同义表达建议，用于修正智能体的意图识别能力。

同义表达建议包含：

- 原始问法、建议标准问题、建议意图和建议理由。
- 来源频次、影响用户数和代表样本。
- 待审核、已通过、已驳回和已导出状态。
- 导出为结构化字典文件，供智能体团队后续人工应用。

意图诊断至少包含：

- 未识别或低置信度。
- 相似问法被识别为不同意图。
- 不同问题被过度合并。
- 同一问题被过度拆分。
- 相同问法在不同时间识别结果不稳定。
- 用户在短时间内重复或换句话询问同一问题。

### 10.6 漏斗分析

默认分析链路：

1. 进入智能体。
2. 发起提问。
3. 获得有效回答。
4. 点击推荐内容或下一步入口。
5. 完成目标业务动作。

支持自定义漏斗步骤和有效时间窗口。系统展示各步人数、转化率、流失率、完成时长和与上期差异。点击流失环节可下钻至用户分群、问题类型和脱敏会话。

### 10.7 路径分析

- 分析用户提问前的来源页面和前置操作。
- 分析提问后的后续页面、业务动作和退出位置。
- 默认展示前后 5 步，支持调整。
- 支持排除无业务意义的曝光、心跳和系统噪声事件。
- AI 解释主要路径和异常路径，但不把路径相关性直接表述为因果。

### 10.8 留存分析

- 默认以首次使用智能体为起点，以再次使用为留存。
- 展示次日、7 日和 30 日留存，支持自定义周期。
- 支持按新老用户、用户岗位、首次问题类型和首次回答效果对比。
- AI 向新手解释留存口径和差异，并提醒节假日、发版和样本规模的影响。

### 10.9 用户分群

- 支持新老用户、用户岗位、工厂/区域、客户类型、活跃度和智能体使用深度等常用分群。
- 支持组合条件创建自定义分群。
- 对比各分群的使用率、人均提问、高频问题、回答效果、留存和后续业务行为。
- AI 自动提醒差异最明显的群体，并生成需要验证的原因假设。

### 10.10 功能采用分析

核心指标：

- 功能使用人数、使用次数和使用会话数。
- 功能渗透率：使用功能的符合条件用户数 / 符合条件用户总数。
- 人均使用频次、使用天数和连续使用周期。
- 首次使用后的再次使用和关键业务行为。
- 上线前后、目标用户与非目标用户的对比。

### 10.11 问题效果分析

对每个标准问题展示：

- 识别成功率。
- 有效回答率。
- 推荐点击率。
- 重复提问率。
- 负向反馈率（数据存在时）。
- 后续业务行为转化率。

系统默认将问题分为“高频高效”“高频低效”“低频高效”和“低频低效”。高频低效问题优先进入改进清单。

### 10.12 异常诊断

自动检测：

- 高频但低识别成功率。
- 高频但低有效回答率。
- 重复提问或换句话追问明显增长。
- 推荐点击高，但后续业务行为低。
- 某个用户分群的识别成功率或留存明显低于整体。
- 新问题快速增长，但当前知识库尚未覆盖。
- 指标相比历史均值或上一周明显异常。

每条异常显示检测口径、当前值、对比值、影响范围、典型样本和建议优先级。

### 10.13 AI 结论和下一步动作

AI 输出必须遵循统一结构：

| 内容 | 要求 |
|---|---|
| 结论 | 用业务语言说明发现了什么 |
| 数据证据 | 列出指标、时间、对比组和代表样本 |
| 影响 | 说明影响哪类用户、多少用户和哪个产品环节 |
| 原因判断 | 区分“数据已支持”和“待验证假设” |
| 下一步动作 | 说明建议改什么、怎么验证 |
| 优先级 | P0、P1、P2 |
| 置信度 | 高、中、低，并说明原因 |

行动建议应优先使用产品和运营可执行的表达，例如“补充这 12 条同义问法”“修改推荐入口文案并对比点击率”，而不是“持续优化用户体验”等空泛建议。

### 10.14 分析报告

- 支持周报、月报、功能上线复盘、问答质量和自定义专项报告。
- 报告结构默认为：结论摘要、核心数据、主要问题、原因判断、下一步动作和附录。
- 用户可编辑 AI 生成内容，添加业务背景和删除不适用结论。
- 支持导出可分享文档和 Excel 明细。
- 报告内的结论支持回到原分析任务和数据证据。

### 10.15 行动清单与效果回看

- 将 AI 建议转换为可编辑的行动项。
- 行动项包含问题、证据、优先级、建议动作、目标指标、状态和预计回看时间。
- 状态包含待评估、处理中、待回看、已完成和已关闭。
- 回看时支持导入新数据，对比动作前后指标。
- AI 生成“改善、无明显变化或变差”的回看摘要，由用户确认。

### 10.16 大模型配置

- 同时支持 GPT、DeepSeek 和 Qwen。
- 支持配置服务商、请求地址、模型名称、API Key、超时时间和重试次数。
- 支持默认模型、备用模型和按任务指定模型。
- 主模型不可用时可切换备用模型，并记录实际使用的模型。
- 三类模型使用统一输入和输出结构，切换模型不改变页面和报告格式。
- 记录调用任务、模型、耗时、成功状态和估算费用。
- API Key 必须加密保存，不出现在页面、日志或报告中。

## 11. 页面结构

| 一级页面 | 核心内容 |
|---|---|
| AI 工作台 | 新建分析、最近任务、异常提醒、待办行动 |
| 数据管理 | 文件导入、字段映射、数据质量、导入模板 |
| 问答洞察 | 高频问题、真实问法、同义表达、意图诊断、问题效果 |
| 产品分析 | 漏斗、路径、留存、用户分群、功能采用 |
| 异常诊断 | 异常列表、影响、证据、原因假设和建议 |
| 报告与行动 | 分析报告、行动清单、效果回看 |
| 设置 | 指标口径、事件映射、模型配置、数据安全 |

## 12. 数据要求

### 12.1 最小字段

| 字段 | 必需性 | 用途 |
|---|---|---|
| user_id | 必需 | 脱敏用户标识，用于人数、路径和留存 |
| event_time | 必需 | 事件时间 |
| event_name | 必需 | 页面访问、提问、获得回答、点击推荐、完成业务动作等 |
| session_id | 建议 | 还原会话和行为路径 |
| page_or_feature | 建议 | 定位事件所在页面和功能 |
| question_id | 问答分析必需 | 唯一问题标识 |
| question_text | 问答分析必需 | 用户真实问法 |
| recognized_intent | 建议 | 当前识别意图 |
| intent_confidence | 建议 | 识别置信度 |
| answer_status | 建议 | 成功、无答案、失败、超时等 |
| recommendation_id | 可选 | 推荐展示或点击 |
| business_result | 可选 | 后续业务动作及结果 |
| user_properties | 可选 | 岗位、工厂/区域、客户类型等分群属性 |

### 12.2 数据边界

- 无稳定 user_id 时，不生成用户留存和跨会话结论。
- 无完整 event_time 或 session_id 时，路径和重复提问仅作参考。
- 无业务结果事件时，不生成后续业务转化结论。
- 无用户属性时，不生成对应用户分群结论。
- 样本量不足时，结论必须标注“样本不足”，不给出强判断。

## 13. 核心指标口径

| 指标 | 默认定义 |
|---|---|
| 智能体使用人数 | 分析期内至少发起一次有效提问的去重用户数 |
| 有效提问数 | 排除空文本、纯符号、明确测试和重复上报后的提问数 |
| 识别成功率 | 成功返回有效意图的提问数 / 有效提问数 |
| 有效回答率 | 成功返回可用回答的提问数 / 有效提问数 |
| 推荐点击率 | 点击推荐的提问数 / 展示推荐的提问数 |
| 重复提问率 | 设定时间内，同一用户再次询问同一标准问题的首次提问数 / 有效提问数 |
| 功能渗透率 | 使用功能的符合条件用户数 / 符合条件用户总数 |
| N 日留存率 | 首次使用后第 N 日再次使用的用户数 / 首次使用用户数 |
| 后续业务转化率 | 归因时间内完成目标动作的提问用户数 / 提问用户数 |

重复提问时间、转化归因时间、有效问题和有效回答的具体规则支持配置，且在报告中可查看。

## 14. AI 使用边界

### 14.1 AI 负责

- 理解用户的业务问题。
- 推荐分析方法、指标和对比方式。
- 聚类真实问法、归纳标准问题、提取同义表达。
- 解释数据差异、趋势和异常。
- 生成报告初稿和可执行建议。

### 14.2 AI 不负责

- 不编造数值或补齐不存在的数据。
- 不把相关性直接当作因果。
- 不在数据不足时生成确定结论。
- 不自动发布意图、同义表达或知识库变更。
- 不替代产品和运营人员做最终优先级决策。

## 15. 权限与数据安全

- 平台仅监听 `127.0.0.1`，不得配置为 `0.0.0.0`；Windows 防火墙不需要为平台开放入站端口。
- 本次不建设平台登录系统，访问权限由目标电脑的 Windows 登录、锁屏和磁盘权限控制。
- 指定使用者可创建分析、编辑报告、导出结果，并维护模型密钥、指标口径和事件映射。
- 用户 ID 必须匿名化，原始问法中的手机号、邮箱、客户名称等信息在发送给外部模型前脱敏。
- 根据企业数据规范确定 GPT、DeepSeek、Qwen 可使用范围；不允许数据出域时，必须使用合规的企业或私有部署服务。
- 本地保存数据不等于完全离线：只要使用云端模型 API，就会发生经过脱敏的数据出域；关闭模型调用后，客观指标分析仍可在本地完成。
- API Key 不写入代码、上传文件、日志或报告，优先存入 Windows 凭据管理器；无法使用时再采用仅当前 Windows 用户可读取的加密配置。
- 数据导入、导出、模型配置和重要口径修改需记录审计日志。
- 原始数据、分析结果和模型调用日志按公司内部规定设置保留期限。

## 16. 异常处理

- 数据不完整时，明确说明影响哪些分析，不强行生成报告。
- 大模型超时或不可用时，系统自动重试或使用备用模型，保留已完成的指标计算。
- 模型输出结构不完整或无法通过业务规则校验时，不写入正式结果。
- 文件重复导入时给出提示，避免指标重复计算。
- 分析任务支持分批处理、进度查看、失败重试和断点继续。

## 17. 非功能需求

### 17.1 易用性

- 新手用户不需要理解 SQL、统计脚本或模型提示词。
- 所有专业指标提供简短业务解释和口径说明。
- 常见任务可通过模板完成，用户不需要从空白页面开始。
- 用户可随时查看 AI 为什么选择某种分析方法。

### 17.2 性能

- 建议验收基线：单次导入 50 万条事件明细时，不含外部模型等待时间的常规指标分析在 15 分钟内完成。
- 已生成的分析页面和报告在 3 秒内展示主要内容。
- AI 任务超过 30 秒时显示进度，不让用户在空白页面等待。

### 17.3 可追溯性

- 保留数据版本、指标规则版本、模型名称、模型版本和分析时间。
- AI 结论可追溯到指标和脱敏样本。
- 用户对聚类、标准问题和结论的人工修改需保留记录。

### 17.4 本机运行稳定性

- 正式运行时只监听 `127.0.0.1`，默认统一入口为 `http://127.0.0.1:8000`。
- 目标电脑重启后，通过桌面快捷方式或启动脚本恢复平台，不要求用户输入开发命令。
- 平台停止后不应继续占用端口或后台计算资源。
- 数据目录、配置和模型密钥与程序目录分离，升级程序时不覆盖业务数据。
- 提供备份、恢复、清理和卸载说明；卸载程序与删除业务数据必须是两个独立动作。

## 18. 验收标准

### 18.1 新手独立完成分析

- 未使用过 SQL 的产品经理可根据页面引导导入标准样例数据。
- 用户可用自然语言提出业务问题，并在无数据分析师协助的情况下完成至少一种标准分析。
- 系统能说明推荐的方法、必需数据、指标口径和结果限制。
- 用户可完成结果复核、报告编辑和导出。

### 18.2 问答洞察

- 每个高频标准问题可查看前 10～20 个真实问法。
- 真实问法包含频次、人数、占比、识别意图和回答状态。
- 可人工合并、拆分和调整标准问题。
- 可生成、审核和导出同义表达字典。
- 可定位未识别、低置信度、意图混淆、过度合并、过度拆分和重复提问。

### 18.3 产品分析

- 在数据条件满足时，可完成漏斗、路径、留存、用户分群、功能采用、问题效果和异常诊断。
- 指标与明细数据重新计算的结果一致。
- 数据不足时，明确提示不能生成的结论及原因。
- 主要结论可下钻到用户分群、标准问题或脱敏会话。

### 18.4 AI 结论

- 每条结论包含数据证据、影响、原因判断、下一步动作、优先级和置信度。
- 结论中的数值与系统计算指标一致。
- 无数据依据的原因不得表述为已证实事实。
- 动作建议必须具体，并包含验证指标或回看方式。

### 18.5 多模型

- GPT、DeepSeek、Qwen 均可独立完成问法聚类、同义表达生成和结论生成。
- 管理员可设置默认模型、备用模型和指定任务模型。
- 主模型失败后可按配置切换备用模型，不丢失原任务。
- 三类模型输出通过统一格式和业务规则校验。
- 可查看调用模型、耗时、成功状态和估算费用。
- 使用固定样本集完成三类模型的上线前对比。

### 18.6 本机部署

- 在一台全新的 Windows 目标电脑上，按部署教程可完成环境检查、安装、启动和停止。
- 目标电脑浏览器可通过 `http://127.0.0.1:8000` 打开平台，其他电脑无法通过该地址访问。
- 未配置任何模型 API Key 时，文件导入和客观指标分析可运行，AI 功能明确提示尚未配置。
- 关闭平台后，对应本地进程停止；卸载程序时可选择保留或单独删除 `D:\FactoryAgentData`。
- 重装或升级后，可通过备份恢复数据、任务、报告和配置，但不在明文备份中包含 API Key。

## 19. 完整交付范围

本项目所有功能一次上线和验收，不拆分产品版本或阶段交付。项目内部可并行开发，但不形成对用户的分批交付。

统一交付包含：

- AI 工作台和新手分析引导。
- 离线数据导入、字段映射、数据质量和导入模板。
- 高频问题、前 10～20 个真实问法、同义表达和意图诊断。
- 漏斗、路径、留存、用户分群、功能采用、问题效果和异常诊断。
- AI 结论、分析报告、行动清单和效果回看。
- GPT、DeepSeek、Qwen 三类模型接入、切换、降级和调用统计。
- 权限、审计、数据脱敏、异常处理和性能要求。
- Windows 目标电脑本机部署包、启动/停止脚本、桌面入口和备份恢复教程。

在样例数据、部署环境和三家模型账号齐备的前提下，建议整体开发与验收周期为 8～12 周。详细排期根据实际数据量、部署方式和人员投入确认。

## 20. 产品成功指标

- 新手产品经理独立完成标准分析的比例。
- 从数据导入到生成首份报告的平均时间。
- 周报和专项分析的人工耗时降低情况。
- AI 生成结论被用户保留、修改和删除的比例。
- AI 建议转换为实际行动项的比例。
- 高频标准问题的真实问法覆盖率。
- 意图未识别率、低置信度率和重复提问率的变化。

具体目标值需在上线前用现有工作方式建立基线，不在无基线时预设失真数字。

## 21. 风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| 导入数据字段不完整 | 部分分析无法完成 | 导入前检查，明确缺失字段和可用分析范围 |
| 用户标识不统一 | 路径和留存不准确 | 建立统一匿名用户标识和合并规则 |
| 新手盲信 AI 结论 | 产生错误决策 | 强制展示数据证据、置信度和待验证假设 |
| AI 聚类或归纳错误 | 错误合并问题或产生错误字典 | 支持人工拆分、合并、驳回和备核 |
| 原始问法包含敏感数据 | 隐私和数据出域风险 | 发送模型前脱敏，按公司规定选择合规模型服务 |
| 模型限频、中断或费用过高 | 分析延迟或成本不可控 | 主备模型、分批处理、预算限制和调用统计 |
| 指标口径不统一 | 不同人员对结论理解不一致 | 口径配置化、版本化，报告中显示实际口径 |

## 22. 开发前待确认事项

1. 工厂智能体当前可导出哪些问答、用户行为和业务结果字段。
2. 默认漏斗中“完成目标业务动作”的具体定义。
3. 是否存在可跨会话关联的稳定匿名 user_id。
4. 当前意图标签、标准问题和知识库是否有可导出数据。
5. GPT、DeepSeek、Qwen 使用哪些账号、请求地址和模型版本。
6. 数据脱敏、数据出域、存储位置和保留时间要求。
7. 报告需要导出的文档格式和内部分享方式。
8. 建议选择 3～5 名不同经验的产品和运营人员参与验收，其中至少 2 名为新手产品经理。
